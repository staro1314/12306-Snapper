# Fast 12306 网页版实施基线

## 目标与边界

用户在 12306 官方 Chrome 窗口扫码并完成人工安全核验，在本地网页配置普通票任务。系统负责等待起售、只读查询、候选排序、串行提交、排队查单、拆单恢复和本机强提醒，自动化成功终点是官方真实 `PAYMENT_PENDING`；支付、取消和任何验证码/App/短信核验始终由用户完成。

- 仅限个人 Windows 本机、单账号；服务只监听回环地址。
- 首版不支持候补和自动支付。
- 真实提交默认关闭，必须在用户指定安全测试车次后显式设置 `FAST_12306_ENABLE_REAL_SUBMISSION=1`。
- 不保存密码、完整证件号、Cookie、动态令牌或原始提交体到 SQLite 和普通日志。

## 已实现架构

- Vue 3 + TypeScript 单页分层界面；Rust/Axum 本地 API；SQLite 状态存储。
- 持久化 Playwright Chrome 负责扫码登录、核验、官方页面和人工接管。
- 当前官网观测形成 `web-2026-09-17-readonly-v1` 协议配置。
- 当前官方能力：登录检查、脱敏乘车人、站点、起售时刻、余票、订单初始化、预检查、确认排队、排队结果、未支付订单核对。
- 多任务、最多五个路线组、最多五名乘车人、每组最多五个车次和五个席别。
- 稳定优先级：任务 → 路线 → 日期 → 车次 → 席别 → 创建顺序。
- 起售临近时 25ms 本地调度；同一路线查询结果在内存中决策；路线轮询不短于 5 秒。
- 查询批次最多两个路线组；真实订单由浏览器服务账号级互斥锁全局串行。
- 拆单必须显式授权；每段提交前重新查询；当前段未知时立即停止后续段并先查单。
- 订单快照、当前任务和乘车人冲突任务在一个 SQLite 事务中提交。
- 进程在订单阶段中断时，重启自动转入 `UNKNOWN_RECONCILING`，不直接重提。
- 登录失效、核验、限流、协议未知或提交异常触发全局安全暂停。
- 本机提醒包括页面警报、系统通知、循环声音、标题闪烁、确认静音和打开官方订单页。
- 记录调度误差、查询往返、候选决策、订单初始化与提交耗时；测试演练与官方运行记录明确区分来源。

## 自动执行链路

```text
扫码登录 → 同步乘车人 → 配置任务 → 查询并确认官方起售时刻
→ 通知、登录、核验、冲突订单和查询页预热
→ READY → ARMED → T0 查询 → 内存候选排序
→ 提交前复查 → ORDER_INITIALIZING（持久化）
→ ORDER_SUBMITTING（持久化）→ 确认排队 → 查单
→ PAYMENT_PENDING / PARTIAL_PAYMENT_PENDING → 强提醒
```

任何超时或未知响应：

```text
停止全部新提交 → UNKNOWN_RECONCILING → 查询官方未支付订单
→ 匹配到订单则保存待支付快照；否则保持停止并要求人工决定
```

## 验证门禁

已自动验证：Rust 状态机/拆单/调度/截止/事务/冲突/崩溃恢复测试，TypeScript 类型检查与 Vite 生产构建，浏览器服务语法检查，真实提交关闭门禁，非本机网页来源拒绝。

已对当前官网只读验证：持久化登录恢复、脱敏乘车人同步、官方起售时刻、北京南至上海虹桥余票查询、订单初始化页面和未支付订单为空的核对结果。

最终真实提交验收必须由用户指定：

1. 非热门且余票充足的日期、路线、车次、席别和乘车人；
2. 明确同意本次测试会创建真实待支付订单；
3. 用户在官方页面自行取消或支付；
4. 验证无重复订单、排队查单、通知延迟和本地状态一致性后，才进入 EXE 打包阶段。
